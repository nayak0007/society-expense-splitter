{
  "name": "@ses/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Zod schemas defining the API contract, shared by client and server",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ses/domain": "workspace:*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
