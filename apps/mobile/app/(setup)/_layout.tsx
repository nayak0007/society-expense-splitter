/**
 * Setup group layout — placeholder until the society setup flow lands
 * (Phase 3).
 */
import { Stack } from 'expo-router';

export default function SetupLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
