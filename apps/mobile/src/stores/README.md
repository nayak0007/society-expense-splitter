# src/stores

Zustand stores land here: `auth.store.ts`, `society.store.ts`, `sync.store.ts`,
`ui.store.ts`, `draft.store.ts` (SAD §4.2).

`activeSocietyId` will be persisted here and injected into every API call and
query key (PRD §3.1). Nothing is implemented yet.
