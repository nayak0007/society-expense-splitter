# src/components/layout

Screen scaffolding primitives (SAD §4.2): `Screen`, `Header`, `TabBar`,
`KeyboardAvoider`. Every route screen renders inside `Screen` so safe-area,
keyboard and scroll behaviour are consistent app-wide.
