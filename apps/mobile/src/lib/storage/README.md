# src/lib/storage

Wrappers for local persistence land here:

- `mmkv.ts` — fast key-value store (UI state, drafts)
- `secure.ts` — expo-secure-store wrapper (tokens, per SAD §3.1)

Nothing is implemented yet — folders follow SAD §4.2 so features have a home.
